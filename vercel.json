{
  "version": 2,
  "installCommand": "if [ -d client ]; then cd client && npm install; else npm install; fi",
  "buildCommand": "if [ -d client ]; then cd client && npm run build && mkdir -p ../dist && cp -R dist/. ../dist/; else npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://task-man-gray.vercel.app/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
